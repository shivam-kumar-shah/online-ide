import React from "react";

type Props = {};

const Editor = (props: Props) => {
  return (
    <div className="border border-font-secondary rounded-md p-3 w-full h-full resize-x">
      Editor
    </div>
  );
};

export default Editor;
